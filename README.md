# murphy

In-world occurrence generator (skeleton, template for future microservices).
Publishes `budowac.murphy.<gameid>.occurrence`; never applies to sim authority.

Env: `PORT`, `GAME_ID`, `NATS_URL`, `DATABASE_URL`, `OCCURRENCE_INTERVAL_SEC`.
